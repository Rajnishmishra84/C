#include <stdio.h>

int main() {

     int n
     printf("Enter a number: ");
     scanf("%d", &n)

     //* Ternary Operator
     //* exp1 ? exp2: exp3

     n%2==0 ? printf("Even number") : printf("Odd number");
     retrun 0;
}